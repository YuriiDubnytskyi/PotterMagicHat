import { StyleSheet } from 'react-native';

export default StyleSheet.create({
  container: {
    display: 'flex',
    alignItems: 'center',
    backgroundColor: 'lightblue',
    padding: 10,
    minWidth: '25%',
    borderWidth: 1,
    borderRadius: 5,
  },
});
